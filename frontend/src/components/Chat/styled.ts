import styled from "styled-components";

const ChatContainer = styled.div`
  width: 100%;
  height: 100%;
  display: flex;

  .Container-left {
    width: 30%;
  }

  .Container-right {
    width: 70%;
  }
`;

export default ChatContainer;
